const fetch = require('node-fetch');

const getPlayers = (req, res, next) => {
  const API_KEY = 0;
  const API_URL = '';
};

const updatePlayer = (req, res, next) => {

};

module.exports = {
  getPlayers,
  updatePlayer,
}